[package]
name = "os"
version = "0.1.0"
edition = "2021"
authors = ["Hilal Morrar <hilalmorrar@gmail.com>"]

[profile.dev]
panic = "abort" # disable stack unwinding on panic

[profile.release]
panic = "abort" # disable stack unwinding on panic
